[package]
name = "ov_inference"
version = "0.1.0"
edition = "2021"

[dependencies]
openvino = { version = "0.10", features = ["runtime-linking"] }
ndarray = "0.16"
ndarray-npy = "0.9"
anyhow = "1"

[profile.release]
opt-level = 3
lto = "thin"
